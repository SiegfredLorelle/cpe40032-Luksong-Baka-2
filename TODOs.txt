TODOs


currently:
add score when dagger hit obstcles
add meat when dagger hit cow
add sfx when dagger hit obstacles
use rotate script when throwning bombs
increase speed of projectiles (dagger and bomb) when on dash



powerup suggestions:
- triple jump (timer)
- fly (timer)

shakescreen kapag nabangga
lives (may tatlong buhay lang)

fix bug where idle animation bugs on restart
different powerup indicator (change color maybe?)
change difficulty scaling (not of arbitrary values)
add ceiling
fix direction when throwing (para d sumayad sa sahig ung binabato)
bg change
elevate floor
add lives
ui
pause mechanic
add git attributes
